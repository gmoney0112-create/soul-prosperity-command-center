{
  "name": "soul-prosperity-command-center",
  "version": "1.0.0",
  "private": true,
  "description": "Soul Prosperity Funnel Command Center dashboard for the $7 eBook offer ladder.",
  "scripts": {
    "dev": "serve . -l 3000 --no-clipboard",
    "start": "serve . -l 3000 --no-clipboard",
    "validate": "node scripts/validate.js",
    "smoke:serverless": "node scripts/smoke-serverless.js",
    "check": "node --check app.js && node --check config.js && node --check api/_lib/ghl.js && node --check api/ghl/health.js && node --check api/ghl/oauth/callback.js && node --check api/ghl/webhook.js && node --check scripts/smoke-serverless.js && node scripts/validate.js && node scripts/smoke-serverless.js"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
